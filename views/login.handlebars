<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>{{title}}</title>
</head>
<body>
   <div class="loginWrapper">
       <div id="loginForm" class="formUser">
            <div class="formWrap">
               <h2 class="formTitle">Login</h2>
               <form action="/login" method="POST">
                  <input type="text" class="input" id="username" name="emailPhone" placeholder="Email / Phone number">
                  <input type="password" class="input" id="loginPass" name="loginPass" placeholder="Password">

                  {{#each errorMessages}}
                     <div class="errorMsg" style={{this.colour}}>{{this.description}}</div>
                  {{/each}}
                  <input class="submitForm" type="submit" value="Login">
               </form>
            </div>
      </div>
   </div>
</body>
</html>